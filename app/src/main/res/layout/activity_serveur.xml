<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/BleuFonce2PrimaryColor">

    <!-- Top Bar -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:background="@color/BleuFoncePrimaryColor"
        android:padding="14dp">

        <!-- Back Button -->
        <ImageView
            android:id="@+id/backButton"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:src="@drawable/ic_back"
            android:contentDescription="Back"/>

        <TextView
            android:id="@+id/statsText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Mes serveurs"
            android:textColor="#D0D0D0"
            android:layout_marginStart="12dp"
            android:fontFamily="sans-serif"
            android:textSize="20sp" />

        <View
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:layout_weight="1" />

        <!-- Bouton créer serveur en bas -->
        <ImageView
            android:id="@+id/btnVoirServerDisponible"
            android:layout_width="30dp"
            android:layout_height="32dp"
            android:layout_gravity="center"
            android:src="@drawable/ic_server_decouvrir" />

    </LinearLayout>

    <!-- ===== CONTENU PRINCIPAL ===== -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:orientation="horizontal">

        <!-- ===== COLONNE SERVEURS ===== -->
        <LinearLayout
            android:layout_width="72dp"
            android:layout_height="match_parent"
            android:orientation="vertical"
            android:background="@color/BleuFoncePrimaryColor">

            <!-- Liste des serveurs -->
            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/serversRecyclerView"
                android:layout_width="match_parent"
                android:layout_height="0dp"
                android:layout_weight="1"
                android:paddingTop="10dp"/>

            <!-- Bouton créer serveur en bas -->
            <ImageView
                android:id="@+id/btnCreateServer"
                android:layout_width="50dp"
                android:layout_height="50dp"
                android:layout_gravity="center"
                android:layout_marginBottom="16dp"
                android:src="@drawable/ic_create_server"
                android:background="@drawable/bg_circle"
                android:padding="10dp"
                android:scaleType="centerInside"/>

        </LinearLayout>



        <!-- ===== ZONE SERVEUR ===== -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:orientation="vertical"
            android:padding="12dp">

            <!-- Header -->
            <TextView
                android:id="@+id/serverName"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Async - Community | Programmation"
                android:textColor="@android:color/white"
                android:textSize="17sp"
                android:textStyle="bold"/>

            <TextView
                android:id="@+id/nombreNombreServeur"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="13 638 membres · Communauté"
                android:textColor="#B5BAC1"
                android:textSize="12sp"
                android:layout_marginTop="4dp"/>

            <View
                android:id="@+id/divider"
                android:layout_width="match_parent"
                android:layout_height="2dp"
                android:layout_marginTop="4dp"
                android:layout_marginBottom="10dp"
                android:background="#2b3144" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Discussion"
                android:textColor="#B5BAC1"
                android:textSize="17sp"
                android:layout_marginTop="2dp"/>

            <!-- Conteneur messages -->
            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/serverDiscussionMsgRecyclerView"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:padding="2dp"
                android:clipToPadding="false"/>

            <Button
                android:id="@+id/btnEntrerDiscussionServer"
                android:layout_width="match_parent"
                android:layout_height="56dp"
                android:layout_margin="16dp"
                android:text="Entrer dans la discussion"
                android:textAllCaps="false"
                android:textSize="15sp"
                android:textColor="@android:color/white"
                android:backgroundTint="#5865F2"/>


        </LinearLayout>

    </LinearLayout>


</LinearLayout>
